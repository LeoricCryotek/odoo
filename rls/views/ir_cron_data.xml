<?xml version="1.0" encoding="UTF-8"?>
<odoo>

<data noupdate="1">
        <record id="update_days_remaining_cron" model="ir.cron">
            <field name="name">Update Days Remaining</field>
            <field name="model_id" ref="model_update_days_remaining"/>
            <field name="state">code</field>
            <field name="code">model._update_days_remaining()</field>
            <field name="user_id" ref="base.user_admin"/>
            <field name="active" eval="True"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="doall" eval="False"/>
            <field name="nextcall" eval="(datetime.now() + timedelta(days=1)).replace(hour=0, minute=5)"/>
        </record>
    </data>
</odoo>